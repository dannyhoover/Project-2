
<h1>Save your favorite recipes</h1>
<div class="container">
    <div class="row">
        <div class="col-md-6">
            <h2>recipes you can save: </h2>
            <ul class="list1">
                {{#each recipes}}
                {{#unless saved}}
                {{> recipes/recipe-block save=true}}
                {{/unless}}
                {{/each}}
            </ul>
        </div>
        <div class="col-md-6">
            <h2>recipes that have been saved: </h2>
            <ul class="list2">
                {{#each recipes}}
                {{#if saved}}
                {{> recipes/recipe-block save=false}}
                {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12 text-center">
            <h2>Add a new recipe:</h2>
            <form class="create-form">
                <div class="form-group">
                    <label for="ca">recipe Name:</label>
                    <input type="text" id="ca" name="name">
                </div>
                <div class="form-group">
                    <label for="saved">Has the new recipe been saved yet?</label><br>
                    <input type="radio" name="saved" value="1" checked> Yes<br>
                    <input type="radio" name="saved" value="0"> No
                </div>
                <button type="submit">Add recipe</button>
            </form>
        </div>
    </div>
</div>